apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-rest.fullname" . }}
  labels:
    {{- include "spring-rest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-rest.selectorLabels" . | nindent 4 }}
